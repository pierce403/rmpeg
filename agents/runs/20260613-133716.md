# 2026-06-13 13:37:16 Autoslop Attempt

Objective: improve Phase 1 strict media matches with conservative image metadata probing.

Change:

- Added TGA metadata probing for TGA 2.0 files with the `TRUEVISION-XFILE` footer.
- Added TIFF metadata probing from classic TIFF IFD `ImageWidth` and `ImageLength` tags.
- Updated README support wording and notes for the observed FFmpeg behavior.

Expected local corpus impact from `site/data/upstream-samples.json`:

- 11 TGA failures become strict matches.
- 11 valid TIFF failures become strict matches.
- `probe-format/tiff-not-mpegts` remains rejected because local ffprobe accepts it despite an out-of-range IFD offset and reports 0x0 dimensions.
- Expected gain: 22 / 2178 ffprobe-accepted files = 1.010 percentage points.

Observed oracle checks:

- A script comparing the 22 target files against local `ffprobe` matched format, codec, width, height, and normalized duration.
- A script scanning all current ffprobe-rejected corpus files found no files matching the new TGA/TIFF guards.

Blocked verification:

- `cargo fmt --check` and `cargo test -p rmpeg-format` could not run because `cargo`, `rustc`, `rustfmt`, and `cargo-clippy` resolve to `/snap/bin/*`, and Snap refused to execute under this sandbox with:

```text
snap-confine has elevated permissions and is not confined but should be. Refusing to continue to avoid permission escalation attacks
Please make sure that the snapd.apparmor service is enabled and started.
```

