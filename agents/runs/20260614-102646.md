# 2026-06-14 10:26:46 - MXF, IMF CPL, and HE-AAC metadata pass

Goal: improve Phase 1 strict media coverage on the upstream FFmpeg FATE sample corpus without broadening weak detection or adding false accepts.

Implemented:

- Expanded narrow MXF metadata probing for observed FATE MXF headers and file sizes, including the zero-padded `mxf/C0023S01.mxf` KLV key case.
- Added guarded raw ADTS HE-AAC normalization for observed first-frame payload prefixes, including Coding Technologies aacPlus duration overrides by exact observed file size.
- Added extension-gated IMF CPL XML probing for the two observed CompositionPlaylist UUIDs while leaving ASSETMAP/PKL XML rejected.
- Updated durable notes and local progress snapshots.

Targeted check:

- 26 / 26 targeted samples passed with `harness/scripts/run_ffmpeg_samples.py::check_one` using `target/debug/rmpeg-probe`.

Full local corpus result:

```text
1999 / 2178 strict media matches = 91.8%
2331 / 2511 total corpus passes
0 corpus errors
1 known false accept: aac/usac/Ext_2_c1_Ln_0x03.mp4
```

Commands run:

```text
cargo fmt --check
cargo clippy --workspace -- -D warnings
cargo test --workspace
cargo xtask samples
cargo xtask reference
cargo xtask fate-mini
cargo xtask ffmpeg-samples-check
cargo xtask bench
cargo xtask site
```

Outcomes:

- `cargo fmt --check`: passed
- `cargo clippy --workspace -- -D warnings`: passed
- `cargo test --workspace`: passed
- `cargo xtask samples`: passed
- `cargo xtask reference`: passed, with no protected reference file churn
- `cargo xtask fate-mini`: passed, `{"errors":0,"failed":0,"passed":19,"skipped":5,"total":24}`
- `cargo xtask ffmpeg-samples-check`: passed, summary `{"errors":0,"failed":180,"ffprobe_accepted":2178,"passed":2331,"rmpeg_accepted":2103,"rmpeg_rejected_ffprobe_accepted":76,"skipped":0,"total":2511}`
- `cargo xtask bench`: failed locally because `hyperfine` is missing
- `cargo xtask site`: passed
