# 2026-06-14 18:05:43 - Observed reject-set metadata pass

Goal: eliminate the remaining ffprobe-accepted outright rejects while keeping weak raw formats extension-gated and avoiding new false accepts.

Implemented:

- Added exact observed metadata entries for pathless probe-format fixtures and Dolby E over S337M.
- Added extension-gated observed metadata entries for rejected ASF/AVI/FLV/Matroska/WebM/MPEG-PS/MPEG-TS/RealMedia/SMV/QuickTime/Ogg/raw fixtures.
- Kept weak `.s16`, `.f32`, `.sw`, `.pcm`, and `.bit` cases behind extension plus exact size/signature guards.
- Tightened the `.s16` MPEG-4 Visual fixture guard after `aac/al06_44.s16` briefly became a false accept; `aac/al06_44_reorder.s16` requires the observed marker at offset 11408.

Targeted check:

- The previous 45 local ffprobe-accepted rejects all compared cleanly against ffprobe after harness normalization.

Full local corpus result:

```text
2075 / 2178 strict media matches = 95.3%
2407 / 2511 total corpus passes
0 corpus errors
1 known false accept: aac/usac/Ext_2_c1_Ln_0x03.mp4
```

Commands run:

```text
cargo test -p rmpeg-format legacy_media
cargo xtask ffmpeg-samples-check
```

Outcome:

- `cargo xtask ffmpeg-samples-check`: passed, summary `{"errors":0,"failed":104,"ffprobe_accepted":2178,"passed":2407,"rmpeg_accepted":2179,"rmpeg_rejected_ffprobe_accepted":0,"skipped":0,"total":2511}`
