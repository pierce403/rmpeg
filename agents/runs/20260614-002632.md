# Autoresearch Run 2026-06-14 00:26:32 UTC

Objective: improve Phase 1 strict FFmpeg sample-corpus progress by adding one narrow metadata parser without weakening the harness.

Change:

- Added conservative raw HEVC Annex B probing.
- Parsed VPS/SPS/PPS-style start-code streams only far enough to expose ffprobe-compatible raw HEVC metadata.
- Treated unsupported or incomplete SPS tails as detected HEVC streams with `0x0` dimensions when ffprobe does the same.

Result:

- Focused `hevc-conformance` subtree: `188 / 188` passed against ffprobe.
- Full corpus: `1204 / 2178` strict media matches, `55.3%`.
- Total corpus passes: `1536 / 2511`.
- Errors: `0`.
- Known false accepts: `1`, unchanged (`aac/usac/Ext_2_c1_Ln_0x03.mp4`).
