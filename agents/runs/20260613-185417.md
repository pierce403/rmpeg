# 2026-06-13 18:54:17 QuickTime/MOV and DNxHR Probe Slice

Objective: improve Phase 1 strict FFmpeg sample-corpus progress with QuickTime/MOV sample-entry metadata and narrow raw DNxHR probing.

Changes:

- Allowed MP4/MOV probing to find `moov` in ftyp-less QuickTime files that start with `wide`, `mdat`, or `moov`.
- Stopped rejecting files after a complete `moov` if a later truncated media box has an impossible declared size.
- Added codec-name mappings for DNxHD/DNxHR, Hap, ProRes, QuickTime Animation, DV, MPEG-4 video, Vorbis-in-MOV, QuickTime PCM, ADPCM, MACE, and QDM2 sample entries.
- Added `sinf/frma` original-format lookup for protected/encrypted MP4/MOV sample entries.
- Added a narrow raw DNxHD/DNxHR metadata probe for the observed FATE frame-header layout.

Validation:

- Targeted `rmpeg-probe` checks for DNxHD-in-MOV, raw DNxHR, Hap, ProRes, ftyp-less QuickTime audio, truncated-tail MOV, encrypted MOV, and `in24` audio.
- `cargo test --workspace`
- `cargo xtask ffmpeg-samples-check`

Result:

- Full corpus before: `1426 / 2178` strict media matches, `65.5%`.
- Full corpus after: `1482 / 2178` strict media matches, `68.0%`.
- Total corpus passes: `1814 / 2511`.
- Corpus errors: `0`.
- Known false accepts remained `1`: `aac/usac/Ext_2_c1_Ln_0x03.mp4`.

Notes:

- The remaining MOV failures are mostly duration/timing edge cases such as edit lists, fragmented durations, and small QuickTime audio duration deltas.
