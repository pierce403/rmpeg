# Autoresearch Run 20260613-182546

Objective: improve Phase 1 strict FFmpeg sample-corpus progress with MP4 AAC/ALS `esds` metadata probing.

Changes:

- Added AudioSpecificConfig parsing for MP4 `esds` descriptors.
- Parsed AAC Program Config Elements for `channelConfiguration == 0`.
- Applied SBR extension sample rates from AudioSpecificConfig sync extensions.
- Identified MP4 ALS from Audio Object Type 36 and read embedded WAVE metadata for sample rate, channels, and bits per sample.

Validation:

- `cargo fmt`
- `cargo test -p rmpeg-format`
- Targeted `rmpeg-probe` checks for MP4 AAC LC mono, PCE 5.1, PCE 7.1, and MP4 ALS.
- `cargo xtask ffmpeg-samples-check`

Result:

- Full corpus: `1382 / 2178` strict media matches, `63.5%`.
- Total corpus passes: `1714 / 2511`.
- Corpus errors: `0`.
- Known false accepts remained at `1`.
