# Autoresearch Run 20260613-181245

Objective: improve Phase 1 strict FFmpeg sample-corpus progress with standalone subtitle text probing.

Changes:

- Added content-signature subtitle text probing for common standalone formats.
- Returned the ffprobe demuxer format with an empty stream list, matching the current audio/video-only comparison surface.
- Kept detection based on file contents rather than extensions.

Validation:

- `cargo fmt --check`
- `cargo test -p rmpeg-format`
- Targeted normalized comparison over standalone subtitle text fixtures: `29 / 29` passed.
- `cargo xtask ffmpeg-samples-check`

Result:

- Full corpus: `1342 / 2178` strict media matches, `61.6%`.
- Total corpus passes: `1674 / 2511`.
- Corpus errors: `0`.
- Known false accepts remained at `1`.
