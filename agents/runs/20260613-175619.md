# Autoresearch Run 20260613-175619

Objective: improve Phase 1 strict FFmpeg sample-corpus progress with narrow audio metadata fixes.

Changes:

- Made Ogg probing tolerate truncated final pages while using only complete-page granules for duration.
- Added MP4/3GP `samr` and `sawb` audio sample-entry mapping and normalization.
- Normalized Matroska lossy compressed audio bit depth to match ffprobe for Opus/Vorbis/AAC/MP3.

Validation:

- `cargo fmt --check`
- `cargo test -p rmpeg-format`
- Targeted normalized comparison over Vorbis truncated Ogg, AMR-WB 3GP/MP4, and Matroska Opus fixtures: `30 / 30` passed.
- `cargo xtask ffmpeg-samples-check`

Result:

- Full corpus: `1291 / 2178` strict media matches, `59.3%`.
- Total corpus passes: `1623 / 2511`.
- Corpus errors: `0`.
- Known false accepts remained at `1`.
