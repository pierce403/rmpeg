# Autoresearch Run 2026-06-14 00:45:05 UTC

Objective: improve Phase 1 strict FFmpeg sample-corpus progress with WavPack metadata support.

Change:

- Added raw WavPack probing from the block header plus embedded RIFF/WAVE or DSDIFF source metadata.
- Used WavPack storage bits when they exceed WAVE valid bits, matching ffprobe on the 12-bit fixture.
- Added Matroska `A_WAVPACK`/`A_WAVPACK4` codec mapping.

Result:

- Focused `wavpack` subtree: `27 / 27` passed against ffprobe.
- Full corpus: `1257 / 2178` strict media matches, `57.7%`.
- Total corpus passes: `1589 / 2511`.
- Errors: `0`.
- Known false accepts: `1`, unchanged (`aac/usac/Ext_2_c1_Ln_0x03.mp4`).
