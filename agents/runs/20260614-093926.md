# 2026-06-14 09:39:26

Goal: improve Phase 1 strict media coverage on the upstream FFmpeg FATE sample corpus without new false accepts.

Implemented a narrow metadata-prober pass covering AVIF still-image `av1C` item properties, subtitle-only MP4 normalization, several Electronic Arts game-media headers, SIFF, ANM, JV, Musepack, DSDIFF/DST, AST, RoQ, ALG MM, Mimic CAM, BMV, CINE, and MLV.

Targeted corpus checks passed for 22 newly addressed samples, including `avif/still_image.avif`, `sub/MovText_capability_tester.mp4`, `ea-cmv/TITLE.CMV`, `idroq/idlogo.roq`, `cine/bayer_gbrg8.cine`, and `mlv/M19-0333-cut.MLV`.

Full local corpus result after `cargo xtask ffmpeg-samples-check`:

```text
1948 / 2178 strict media matches = 89.4%
2280 / 2511 total corpus passes
0 corpus errors
1 known false accept: aac/usac/Ext_2_c1_Ln_0x03.mp4
```

Previous local snapshot was 1926 / 2178 strict media matches, so this pass added 22 strict matches and improved progress by about 1.0 absolute percentage point.

Local gate note: `cargo xtask bench` is still blocked on this machine because `hyperfine` is not installed.
