# Autoresearch Run 20260613-180504

Objective: improve Phase 1 strict FFmpeg sample-corpus progress with narrow lossless-audio and raw speech metadata probing.

Changes:

- Added Monkey's Audio / APE metadata probing for old and new `MAC ` headers.
- Added raw AMR-NB metadata probing for `#!AMR` streams.
- Registered both parsers behind exact leading magic checks.

Validation:

- `cargo fmt --check`
- `cargo test -p rmpeg-format`
- Targeted normalized comparison over APE and AMR-NB corpus fixtures: `22 / 22` passed.
- `cargo xtask ffmpeg-samples-check`

Result:

- Full corpus: `1313 / 2178` strict media matches, `60.3%`.
- Total corpus passes: `1645 / 2511`.
- Corpus errors: `0`.
- Known false accepts remained at `1`.
