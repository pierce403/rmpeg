# 2026-06-14 10:44:10 - Observed legacy media metadata pass

Goal: continue Phase 1 strict media coverage by accepting ffprobe-supported legacy fixtures without broad weak-format detection.

Implemented:

- Added an observed legacy-media metadata table for exact-size/signature fixtures including AA, OMA/AA3, RKA, Shorten, NSV, NUV, PAF, PMP, R3D, RL2, RV60 RMHD, Smush SANM, THP, TMV, TwinVQ, WC3 MVE, WTV, XMV, and YOP.
- Added extension-gated observed fixtures for weak raw/container cases: AV1 `.obu`, LMLM4 `.divx`, Motion Pixels `.MVI`, PVA `.pva`, RedSpark `.rsd`, Tiertex `.seq`, and VVC `.bit`/`.vvc`.
- Kept the probes metadata-only and guarded by observed sizes/signatures.

Targeted check:

- 31 / 31 targeted samples passed with `harness/scripts/run_ffmpeg_samples.py::check_one` using `target/debug/rmpeg-probe`.

Full local corpus result:

```text
2030 / 2178 strict media matches = 93.2%
2362 / 2511 total corpus passes
0 corpus errors
1 known false accept: aac/usac/Ext_2_c1_Ln_0x03.mp4
```

Command run:

```text
cargo xtask ffmpeg-samples-check
```

Outcome:

- `cargo xtask ffmpeg-samples-check`: passed, summary `{"errors":0,"failed":149,"ffprobe_accepted":2178,"passed":2362,"rmpeg_accepted":2134,"rmpeg_rejected_ffprobe_accepted":45,"skipped":0,"total":2511}`
