# Autoresearch Run 20260613-184350

Objective: improve Phase 1 strict FFmpeg sample-corpus progress with narrow DTS metadata probing.

Changes:

- Added DTS-HD `.dtshd` chunk parsing for `AUPR-HDR` metadata.
- Added raw big-endian DTS core header parsing for sample rate, channels, bitrate-derived duration, and observed ES/HD extension cases.
- Added narrow MPEG-TS PES scanning for DTS payloads and PTS-span duration.
- Kept detection constrained to DTS signatures and DTS-bearing TS payloads.

Validation:

- `cargo fmt`
- `cargo test -p rmpeg-format dts::tests`
- Targeted `rmpeg-probe` checks for DTS-HD 7.1/96 kHz, DTS-HD 5.1/192 kHz/16-bit, raw DTS-ES, and DTS-in-TS fixtures.
- `cargo xtask ffmpeg-samples-check`

Result:

- Full corpus: `1426 / 2178` strict media matches, `65.5%`.
- Total corpus passes: `1758 / 2511`.
- Corpus errors: `0`.
- Known false accepts remained at `1`.
