# Autoresearch Run 20260613-183312

Objective: improve Phase 1 strict FFmpeg sample-corpus progress with narrow RIFF/AVI UtVideo metadata probing.

Changes:

- Added RIFF/AVI header walking for `LIST`/chunk structures.
- Parsed `strh` stream headers for video fourcc and `dwLength * dwScale / dwRate` duration.
- Parsed `strf` BITMAPINFOHEADER dimensions and compression fourcc.
- Mapped only the observed UtVideo fourccs used by the FATE UtVideo fixtures.

Validation:

- `cargo fmt`
- `cargo test -p rmpeg-format avi::tests`
- Targeted `rmpeg-probe` checks for UtVideo RGB, RGBA, and YUV444 AVI fixtures.
- `cargo xtask ffmpeg-samples-check`

Result:

- Full corpus: `1404 / 2178` strict media matches, `64.5%`.
- Total corpus passes: `1736 / 2511`.
- Corpus errors: `0`.
- Known false accepts remained at `1`.
