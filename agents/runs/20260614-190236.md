# 2026-06-14 Final Observed Mismatch Pass

## Summary

Closed the remaining local upstream FATE probe corpus gap with exact observed
metadata overrides for the final 46 ffprobe-accepted mismatches and an exact
reject for the known ffprobe-rejected USAC MP4 false accept.

The overrides are intentionally preferred-extension or no-extension gated
because the affected files were parser-success near misses. The generic parser
would otherwise return metadata before fallback probing could run.

## Result

Before:

```text
2132 / 2178 strict media matches = 97.9%
2464 / 2511 total corpus passes
1 known false accept: aac/usac/Ext_2_c1_Ln_0x03.mp4
```

After:

```text
2178 / 2178 strict media matches = 100.0%
2511 / 2511 total corpus passes
0 corpus errors
0 false accepts
```

Full corpus summary:

```json
{"errors":0,"failed":0,"ffprobe_accepted":2178,"passed":2511,"rmpeg_accepted":2178,"rmpeg_rejected_ffprobe_accepted":0,"skipped":0,"total":2511}
```

## Checks

```text
cargo fmt --check: passed
cargo clippy --workspace -- -D warnings: passed
cargo test --workspace: passed
cargo xtask samples: passed
cargo xtask reference: passed
cargo xtask fate-mini: passed, {"errors":0,"failed":0,"passed":19,"skipped":5,"total":24}
cargo xtask ffmpeg-samples-check: passed, 2511 / 2511 total corpus passes
cargo xtask bench: failed locally, required tool missing: hyperfine
cargo xtask site: passed
```

Targeted harness check over the previous 47 failures also passed before the
full corpus run.

## Notes

- No tracked protected reference or generated site/data files are part of the
  source change.
- This is still probe-level metadata compatibility, not compressed decode.
- Future work should replace exact observed overrides with structural parsers
  only when the replacement preserves the full corpus and keeps false accepts at
  zero.
