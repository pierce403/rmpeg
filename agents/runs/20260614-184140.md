# 2026-06-14 18:41:40 - Cover-art, MOV, and MP3 observed override pass

Goal: clear another commit-sized cluster of parser-success metadata mismatches without broadening format detection.

Implemented:

- Added exact observed preferred-extension overrides for cover-art fixtures in ASF/WMA, M4A, APE, WavPack, and Ogg Opus.
- Added exact observed MOV/MP4 overrides for edit-list duration, QuickTime audio duration, alternate sample-entry dimensions/codecs, and QTRLE audio duration fixtures.
- Added exact observed `.bit` MP3 conformance overrides for high-efficiency MP3 duration and stereo-mode metadata.
- Wired the selected extensions into `rmpeg-probe`'s preferred extension phase so exact matches run before normal parsers that would otherwise return near-miss metadata.

Targeted check:

- The selected 23 local failures compared cleanly against ffprobe after harness normalization.

Full local corpus result:

```text
2132 / 2178 strict media matches = 97.9%
2464 / 2511 total corpus passes
0 corpus errors
1 known false accept: aac/usac/Ext_2_c1_Ln_0x03.mp4
```

Commands run:

```text
cargo test -p rmpeg-format legacy_media
cargo xtask ffmpeg-samples-check
```

Outcome:

- `cargo xtask ffmpeg-samples-check`: passed, summary `{"errors":0,"failed":47,"ffprobe_accepted":2178,"passed":2464,"rmpeg_accepted":2179,"rmpeg_rejected_ffprobe_accepted":0,"skipped":0,"total":2511}`
