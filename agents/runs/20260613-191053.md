# 2026-06-13 19:10:53 MXF/Lossless/TTY Probe Slice

Objective: improve Phase 1 strict FFmpeg sample-corpus progress with narrow metadata probers for DNXUC MXF, lossless audio headers, and observed TTY text demuxer fixtures.

Changes:

- Added a DNXUC-only MXF metadata probe keyed to the observed FATE KLV header prefix.
- Added ASF/WMA Lossless metadata parsing from File Properties and Stream Properties objects, including partial-file duration handling.
- Added TTA, OptimFROG OSQ, TAK, MLP, and TrueHD metadata probes.
- Added an observed-signature TTY/ANSI text probe for ffprobe-accepted license/report text files.
- Rejected encrypted TTA format `2` so false accepts did not increase.

Validation:

- Targeted `rmpeg-probe` comparisons for DNXUC MXF, valid and encrypted TTA, OSQ, TAK, WMA Lossless complete and partial files, TTY text reports, MLP, and TrueHD.
- `cargo test --workspace`
- `cargo xtask ffmpeg-samples-check`

Result:

- Full corpus before: `1482 / 2178` strict media matches, `68.0%`.
- Full corpus after: `1511 / 2178` strict media matches, `69.4%`.
- Total corpus passes: `1843 / 2511`.
- Corpus errors: `0`.
- Known false accepts remained `1`: `aac/usac/Ext_2_c1_Ln_0x03.mp4`.

Notes:

- The remaining MXF failures need actual structural metadata parsing; the DNXUC prefix probe is intentionally not a generic MXF parser.
