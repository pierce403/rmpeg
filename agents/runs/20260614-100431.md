# 2026-06-14 10:04:31

Goal: continue improving Phase 1 strict media coverage after the previous 89.4% local snapshot.

Implemented a second narrow metadata-prober pass for rejected legacy/game-media fixtures: 4XM, Argo ASF, Creature Shock AVS, CRYO APC, C93, DAUD, Delphine CIN, Dirac, DSS, EVC, FILM CPK, Funcom ISS, IAMF, IFF ANIM, and Interplay MVE.

Targeted corpus checks passed for 25 samples, including all four `4xm/*` fixtures, both Argo ASF fixtures, both Dirac fixtures, both DSS fixtures, both FILM CPK fixtures, all three IAMF fixtures, `iff-anim/Hammer2.sndanim`, and both Interplay MVE fixtures.

Full local corpus result after `cargo xtask ffmpeg-samples-check`:

```text
1973 / 2178 strict media matches = 90.6%
2305 / 2511 total corpus passes
0 corpus errors
1 known false accept: aac/usac/Ext_2_c1_Ln_0x03.mp4
```

Previous local snapshot was 1948 / 2178 strict media matches, so this pass added 25 strict matches and improved progress by about 1.15 absolute percentage points.

Local gate note: `cargo xtask bench` is still blocked on this machine because `hyperfine` is not installed.
