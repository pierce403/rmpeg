[package]
name = "rmpeg-codec"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
rmpeg-core = { path = "../rmpeg-core" }
rmpeg-format = { path = "../rmpeg-format" }
