# rmpeg Notes

This directory holds durable notes about surprising FFmpeg behavior, corpus quirks, and rmpeg implementation traps.

Use `unexpected-quirks.md` for short entries that should survive beyond a single agent run. Keep entries factual:

- what was surprising
- how it was observed
- what it means for rmpeg or the harness

Do not put generated reference outputs, benchmark data, or large media files here.
